% main.tex — TPMS Explorer v7.0 SoftwareX-style manuscript (self-contained, pdflatex)
% 编译：pdflatex main && bibtex main && pdflatex main && pdflatex main
\documentclass[5p,times,authoryear]{elsarticle}
\usepackage{amsmath,amssymb}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{hyperref}
\usepackage{xcolor}

\journal{SoftwareX}

\begin{document}

\begin{frontmatter}

\title{TPMS Explorer: an interactive, gate-verified open ecosystem for triply periodic
minimal surface design, generative implicit fields, multifunctional physics simulation,
and biophysical tissue-ingrowth dynamics}

\author[a]{Anonymous Author}
\address[a]{Independent Open-Source Project}

\begin{abstract}
We present TPMS Explorer v7.0, a browser-based, zero-backend ecosystem for parametric design,
physics simulation, and additive-manufacturing preparation of triply periodic minimal surface
(TPMS) architected materials. The platform couples a watertight Surface Nets meshing pipeline
with thirty-eight continuously executed CI gates plus a navigation quick-check (39-item suite,
$>1000 assertions under strict per-line accounting, each gate carrying a minimum-assertion guard)
claim shipped to the user. Version 7.0 adds five research-grade modules: (i) a sinusoidal-activation
implicit neural field (SIREN) with an exactly periodic Fourier-feature input and an 8-dimensional
latent space over five distilled TPMS experts; (ii) multiaxial anisotropic yield envelopes
(Hill-48, Tsai-Wu, Gurson, Drucker--Prager) with a unified star-convex ray-distance formulation
and an interactive 3D viewer; (iii) a Bloch--Floquet phononic band-structure solver built on
Born--von K\'arm\'an lattice dynamics with realified two-pass deflate-Lanczos eigensolution and
long-wave dynamical-matrix calibration; (iv) a reaction--diffusion tissue-ingrowth engine coupling
quasi-steady oxygen transport, hypoxia-gated osteoblast proliferation, and mineral deposition over
a 28-day horizon; and (v) a level-set topology-optimization loop with Hamilton--Jacobi Godunov
evolution driven by elastic and flow sensitivities under a volume constraint. All modules execute
client-side with zero dependencies beyond Three.js, export to industrial formats (STL, 3MF, GLB,
Abaqus INP, OpenFOAM polyMesh, G-code), and are guarded by behavior-level audit gates whose
calibration traps are documented.
Post-release hardening adds (vi) a deterministic exact-porosity
solver --- analytic-integration root finding with mesh-measured secant validation, reaching 0.26 pp
deviation at resolution 96 --- and (vii) constructive global orientation propagation that reduces
misoriented STL edges to zero by construction, replacing a per-triangle normal-voting scheme that
degraded winding consistency five-fold.
\end{abstract}

\begin{keyword}
TPMS \sep implicit neural representation \sep SIREN \sep yield surface \sep phononic bandgap \sep
tissue engineering \sep level-set topology optimization \sep continuous integration
\end{keyword}

\end{frontmatter}

\section{Impact overview}
Triply periodic minimal surfaces --- first catalogued systematically by Schoen
\cite{schoen1970infinite} --- are today a mainstay of architected-materials design and
bone-scaffold engineering, where matching a target porosity while keeping the lattice
manufacturable is the central numerical problem. TPMS Explorer turns a browser tab into a
research bench for architected materials. The five v7.0
modules extend the platform from ``geometry plus linear physics surrogates'' to generative design,
structural-failure envelopes, wave dynamics, biophysical growth, and closed-loop shape optimization
--- each guarded by an audit gate that executes the same code paths the user sees
(Table~\ref{tab:gates}).

\begin{table}[h]
\centering
\caption{v7.0 modules and their CI gates.}
\label{tab:gates}
\begin{tabular}{@{}llp{5.2cm}l@{}}
\toprule
Stage & Module & Verified invariants & Assertions \\
\midrule
I & SIREN implicit field & exact $2\pi$ periodicity ($2.2\times10^{-15}$); distillation RMSE per
expert; Lipschitz product bound vs.\ sampled gradients; latent-path Lipschitz; water-tight meshes
at anchors and manifold midpoints & 43 \\
II & Yield envelopes & uniaxial analytic anchors; midpoint convexity ($\times200$ per criterion);
isotropic permutation symmetry; Gurson hydrostatic pole; closed-mesh volume by divergence
theorem; safety factor of a half-scaled stress $=2$ & 35 \\
III & Phononic bands & $\Gamma$-point triple translational zero modes; sound-speed self-consistency
(gyroid 2.2\%); per-$k$ branch ordering; time-reversal symmetry $\omega(\mathbf{k})=\omega(-\mathbf{k})$;
diatomic-chain analytic gap; island-count guard & 18 \\
IV & Tissue ingrowth & mass balance $=$ boundary-layer flux $-$ interior consumption (0.000\%);
core-oxygen transport differentiation; $\rho\le\rho_{\max}$; hypoxia gating; mineral monotonicity;
bitwise determinism & 11 \\
V & Level-set optimization & unit-load compliance $\propto 1/\text{reaction}$ monotone under 0.5\%
tolerance; volume Lagrange constraint $\pm$10\%; permeability gain $\times1.50$; water-tight
extraction (open edges $=0$); reinitialization $|\nabla\Phi|\approx1$ & 8 \\
\bottomrule
\end{tabular}
\end{table}

\section{Necessary resources}
\subsection{Hardware}
Any 2020-class laptop. WebGPU is optional (acceleration only); all v7.0 modules run on the CPU path.

\subsection{Software}
Modern Chromium/Firefox/Safari; Node.js $\ge$ 20 for the CI suite; TypeScript 6, Vite 8,
Three.js 0.185 (only runtime dependency).

\section{Method design}
\subsection{SIREN implicit field with exact periodicity}
A 4-layer sinusoidal network in the spirit of SIREN \cite{sitzmann2020siren},
$F_\theta(\mathbf{x})=W_3\sin(W_2\sin(W_1\sin(W_0\,\mathbf{u})))
+\mathbf{b}_3$ consumes the Fourier feature $\mathbf{u}=[\sin x,\cos x,\sin y,\cos y,\sin z,\cos z]$.
Because every coordinate enters only through $\sin/\cos$, $F_\theta$ is \emph{exactly} $2\pi$-periodic
(measured deviation $2.2\times10^{-15}$), which makes cross-period water-tightness a property of the
architecture rather than of the fit. Five experts (Gyroid, Diamond, Lidinoid, Schwarz~P, fractal
trabecular) are distilled offline from the platform's single semantic source of TPMS fields with
Adam (RMSE $0.69\%$--$8.04\%$ at $16^3$, hardest for the multi-scale teacher). A latent code
$\mathbf{z}\in\mathbb{R}^8$ selects experts through
$m=\mathrm{softmax}(-\|\mathbf{z}-\mathbf{A}_i\|^2/2\sigma^2)$ with $\pm3$ Walsh anchor codes
(pairwise Hamming distance 4, cross weight $e^{-72}$). Lipschitz continuity is bounded by
$\prod_i\|W_i\|_2$ (power iteration) and cross-checked against sampled gradient suprema.

\subsection{Multiaxial yield envelopes}
All four criteria are expressed in a unified \emph{ray-distance} form: the distance $r(\hat{\mathbf{n}})$
from the origin to the envelope along a unit direction of principal-stress space. Hill-48
\cite{hill1948theory} (with a
hydrostatic cap), capped Drucker--Prager, and Gurson close to star-convex bodies containing the
origin, hence $r$ is single-valued and the safety factor is exactly $r(\hat{\mathbf{n}})/\|\boldsymbol{\sigma}_0\|$.
Tsai-Wu admits a closed-form quadratic root. Gurson's hydrostatic pole
\cite{gurson1977continuum}
$(2\sigma_0/3)\,\mathrm{arccosh}((1+q^2)/2q)$ with $q$ equal to the porosity provides a strong
analytic anchor. Scaffold-level parameters are derived from the same Gibson--Ashby constants
\cite{gibson1997cellular} used
by the platform's energy-absorption module.

\subsection{Bloch--Floquet phononic bands}
The unit cell is discretized into voxel masses connected by 26-neighbourhood axial springs;
the Bloch--Floquet treatment follows the standard phononic-crystal formalism
\cite{hussein2014review}.
Bloch phases assemble a complex Hermitian $K(\mathbf{k})$, realified as
$\begin{psmallmatrix}A&-B\\B&A\end{psmallmatrix}$ so that all eigensolution machinery is real.
Two-pass \emph{deflate-Lanczos} (full reorthogonalization; the second run is deflated against the
first run's Ritz vectors) recovers both copies of every complex eigenvalue, preserving degeneracies.
Spring stiffness $\kappa$ is calibrated by exact inversion of the long-wave dynamical matrix
$\omega^2_{\mathrm{LA}}=\kappa h^2 k^2 S_x/(mN^3)$, $S_x=\sum \mathrm{d}x^4/\mathrm{d}^2$ --- affine
homogenization is explicitly \emph{not} the long-wave modulus of a central-force network (measured
ratio $0.53$, matching the analytic $0.54$). Under periodic boundary conditions rotations are not
admissible Bloch modes, so the $\Gamma$ point carries exactly three translational zero modes.

\subsection{Tissue ingrowth reaction--diffusion}
Oxygen obeys a quasi-steady diffusion--Michaelis--Menten balance solved by 7-point Jacobi iteration
with no-flux mirrors at solid interfaces and Dirichlet $C_0$ on the perfused boundary layer;
osteoblasts follow hypoxia-gated logistic growth with explicit daily updates; mineral accumulates
where oxygen exceeds a threshold. Operator splitting is justified by $\tau_{O_2}=L^2/D\approx0.05$
days versus a 28-day proliferation horizon. $V_{\max}$ is calibrated to a literature oxygen
penetration depth $L_{\mathrm{pen}}=\sqrt{2DC_0/V_{\max}}\approx0.4$\,mm. The gate verifies the
discrete mass balance (boundary-layer flux $=$ interior consumption, residual $0.000\%$) and
hypoxia gating on a dense-versus-porous pair.

\subsection{Level-set topology optimization}
The TPMS signed field evolves under $\partial\Phi/\partial t+V_n|\nabla\Phi|=0$ (Godunov upwinding)
with $V_n$ assembled from elastic von-Mises sensitivities (fully-stressed) and Darcy pressure-gradient
sensitivities, zero-meaned over \emph{interface} voxels, normalized, and constrained by a volume
Lagrange bias. Classical compliance under a unit service load is proportional to $1/\text{reaction}$
of the displacement-controlled solve --- the $\tfrac12 F\delta$ reading is a softening artefact under
displacement control. Islands are removed by largest-component BFS, $\Phi$ is reinitialized by an
exact 3D Euclidean distance transform, and the final field re-enters the platform's Surface Nets
pipeline through the precomputed-field injection contract, guaranteeing water-tight extraction.

\subsection{Exact porosity solver: analytic-integration root finding}
The default porosity path couples the requested value to a voxel-rank bisection whose caliber
differs from the delivered triangle mesh; measured deviations reached 24 pp for higher-harmonic
families at coarse resolutions. Following the analytic-integration methodology of RegionTPMS
\cite{karakoc2021regiontpms}, the agent CLI now (i) locates $iso^{*}$ on the \emph{analytic} field by
bisection over a deterministic Monte-Carlo integration of $P(F<iso)$ in the unit cell (LCG-seeded,
bit-reproducible), then (ii) builds once with $iso^{*}$ (bypassing the internal bisection) and
applies a single secant correction along the analytic slope to absorb the mesh-versus-analytic
volume gap, reverting to the direct build if the corrected attempt is worse. Measured deviations:
0.26 pp (Diamond, 65\% target, resolution 96) and 1.3--4.3 pp across cubic-symmetric families at
resolution 48; higher-harmonic families at resolution 48 remain representation-limited (9--23 pp)
and are reported honestly with an upgrade hint. A cube + Diamond + resolution-64 combination with
non-manifold edges (nm = 740, down from 1940 under the legacy path) is fail-closed by the
water-tightness gate.

\subsection{Global orientation propagation}
The binary and multi-solid STL writers previously flipped each triangle independently by comparing
geometric normals against the vertex normal field; because that field is unreliable at cap fans and
boundary skins, independent voting fragmented a locally consistent winding (misoriented edges
4536 $\rightarrow$ 23571 after flipping at resolution 96). The writers now call a constructive
orientation pass: register every undirected edge with its two incident triangles, two-color the
triangle adjacency graph by BFS (same-direction neighbours get opposite colors), apply the parity,
and flip the whole mesh once if the divergence-theorem volume is negative. For the closed manifolds
the pipeline guarantees (open = non-manifold = degenerate = 0), misoriented edges are zero by
construction --- verified byte-level on the exported STL (823,500 edges at resolution 96).

\section{Quality control}
Thirty-eight gates plus a navigation quick-check (39-item top-level suite) run via
\texttt{npm run test:all} on three platforms. Every gate loads the shipped TypeScript through a
bundler and asserts behavior-level invariants (no mocked math), and every gate now carries a
\emph{minimum-assertion guard}: a probe experiment showed that an auditor whose assertions are all
neutralized still exits green under the historical \texttt{if (fail) exit} idiom, so each gate
additionally fails when its executed-assertion count drops below a measured baseline. Four
tautological assertions (two \texttt{|| true} disjunctions, a self-multiplying metric check, and a
dead variable that was computed but never asserted) were exposed by a dedicated audit and either
removed or rewritten as real checks; one of them had been masking a wrong closed-form slope in the
Poincaré extension for its entire lifetime. Gate calibration is treated as a first-class
activity: documented false-red episodes (a $\sqrt2$ scaling slip in the auditor von-Mises
conversion; a Gram--Schmidt normalization threshold that silently skipped $10^{-10}$-scale columns;
a cross-implementation static sentinel left unsynced after a formula change) are recorded and their
fixes covered by regression assertions.

\section{Availability}
\subsection{Current code version}
v7.0.0-generative-biophysics (MIT license).

\subsection{Operating requirements}
See Necessary resources; no backend, no telemetry, no build-time secrets.

\subsection{Installation}
Open \texttt{docs/platform/index.html} (static build) or run \texttt{npm install} and
\texttt{npm run dev} in \texttt{tpms/tpms-platform}. CI: \texttt{npm run test:all}.
A deterministic agent CLI (\texttt{tpms/agent/tpms.mjs}) exposes \texttt{list}, 
\texttt{estimate}, and \texttt{mesh} subcommands with JSON output for scripting and
LLM-tool integration.

\begin{thebibliography}{9}

\bibitem[{Karako{\c{c}}(2021)}]{karakoc2021regiontpms}
Karako{\c{c}}, A., 2021. Region based triply periodic minimal surfaces (TPMS) for 3-D printed
multiphase bone scaffolds with exact porosity values. SoftwareX 16, 100835.
\doi{10.1016/j.softx.2021.100835}

\bibitem[{Schoen(1970)}]{schoen1970infinite}
Schoen, A.H., 1970. Infinite periodic minimal surfaces and their crystallography in nature and
the design of catalysts. NASA Technical Note TN D-5541.

\bibitem[{Gibson(1997)}]{gibson1997cellular}
Gibson, L.J., Ashby, M.F., 1997. Cellular Solids: Structure and Properties, 2nd ed. Cambridge
University Press, Cambridge.

\bibitem[{Sitzmann(2020)}]{sitzmann2020siren}
Sitzmann, V., Martel, J.N.P., Bergman, A.W., Lindell, D.B., Wetzstein, G., 2020. Implicit neural
representations with periodic activation functions. In: Advances in Neural Information Processing
Systems 33 (NeurIPS 2020), pp. 7462--7473.

\bibitem[{Hussein(2014)}]{hussein2014review}
Hussein, M.I., Leamy, M.J., Ruzzene, M., 2014. Dynamics of phononic materials and structures:
Historical origins, recent progress, and future outlook. Applied Mechanics Reviews 66(4), 040802.
\doi{10.1115/1.4026911}

\bibitem[{Gurson(1977)}]{gurson1977continuum}
Gurson, A.L., 1977. Continuum theory of ductile rupture by void nucleation and growth: Part I ---
Yield criteria and flow rules for porous ductile media. Journal of Engineering Materials and
Technology 99(1), 2--15. \doi{10.1115/1.3443401}

\bibitem[{Hill(1948)}]{hill1948theory}
Hill, R., 1948. A theory of the yielding and plastic flow of anisotropic metals. Proceedings of
the Royal Society of London. Series A 193(1033), 281--297. \doi{10.1098/rspa.1948.0045}

\end{thebibliography}

\end{document}
