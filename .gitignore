# 本地开发辅助文件（不上线）
tpms/agent_memory/
tpms/.zcode/
tpms/.diag/
.workbuddy/

# 依赖（所有项目的 node_modules）
**/node_modules/

# 构建产物
tpms/tpms-platform/dist/

# 自动化验证过程产物（脚本本身入库，产物不入库）
tpms/.verify/shots/
docs/shots/verify/
# 一次性临时验证脚本（本轮终审目视验证用，非正式回归套件）
tpms/.verify/verify_visual.mjs
tpms/.verify/verify_visual2.mjs
tpms/.verify/verify_platform_frd.mjs
tpms/.verify/verify_platform_all.mjs

# 副项目运行时产物
claude-code-workflow/.trash/
claude-code-workflow/crdt-data/

# 其他
*.zip
